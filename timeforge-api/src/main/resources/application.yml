spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/timeforge_db
    username: postgres
    password: 123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080

security:
  jwt:
    secret: "CHANGE_ME_TO_A_MINIMUM_32_CHAR_SECRET_KEY"
    expiration-minutes: 120
  admin:
    nome: "Administrador"
    email: "admin@timeforge.local"
    senha: "admin123"
